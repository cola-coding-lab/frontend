import { SortExplorerPipe } from './sort-explorer.pipe';

describe('SortExplorerPipe', () => {
  it('create an instance', () => {
    const pipe = new SortExplorerPipe();
    expect(pipe).toBeTruthy();
  });
});
