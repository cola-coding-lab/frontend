import { ExplorerAddFileDirective } from './explorer-add-file.directive';

describe('ExplorerAddFileDirective', () => {
  it('should create an instance', () => {
    const directive = new ExplorerAddFileDirective();
    expect(directive).toBeTruthy();
  });
});
